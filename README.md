# EBAC Shop Mobile Tests (Sauce Labs)
Este projeto executa testes automatizados no app EBAC Shop utilizando o Sauce Labs.

## 🚀 Como rodar

1. Instale dependências:
   ```bash
   npm install
   ```

2. Execute no Sauce Labs:
   ```bash
   npm run test:sauce
   ```

## 🧠 Estrutura
- `test/specs/` — Casos de teste (login, cadastro, compra)
- `test/pageobjects/` — Page Objects
- `app/` — App .aab utilizado nos testes
